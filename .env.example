# API keys — only needed for the cloud providers.
# The default provider (Ollama) runs locally and needs no key.

# For --provider claude
ANTHROPIC_API_KEY=sk-ant-...

# For --provider gemini (get a free key at https://aistudio.google.com/apikey)
GEMINI_API_KEY=...
